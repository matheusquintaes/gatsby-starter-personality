import styled from "styled-components"

export const FooterWrapper = styled.div`
  margin-top: 40px;
  opacity: 0.6;
  color: var(--gray1);

  a {
    font-weight: normal;
    text-decoration: underline;
  }
`
